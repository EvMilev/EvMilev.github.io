let bigger = document.getElementById('circle-name');

function biggerCircle () {
    bigger.style.width = '40rem'
    bigger.style.height = '40rem'
};

bigger.addEventListener('mouseover', biggerCircle);
